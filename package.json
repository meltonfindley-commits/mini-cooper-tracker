{
  "name": "mini-cooper",
  "private": true,
  "scripts": {
    "dev:dashboard": "npm --prefix apps/dashboard run dev",
    "dev:fuel": "npm --prefix apps/fuel-tracker run dev",
    "build:dashboard": "npm --prefix apps/dashboard run build",
    "build:fuel": "npm --prefix apps/fuel-tracker run build"
  }
}
